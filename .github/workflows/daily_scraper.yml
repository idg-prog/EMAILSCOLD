name: Daily UAE Lead Scraper

on:
  schedule:
    # Runs automatically every day at 06:00 UTC (10:00 AM UAE Time)
    - cron: '0 2 * * *'
  # Allows manually triggering the run from the GitHub Actions UI anytime
  workflow_dispatch:

jobs:
  run-scraper:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install google-search-results pandas requests beautifulsoup4 openpyxl

      - name: Execute Scraper Script
        env:
          SERPAPI_KEY: ${{ secrets.SERPAPI_KEY }}
          N8N_WEBHOOK_URL: ${{ secrets.N8N_WEBHOOK_URL }}
        run: python scraper.py
